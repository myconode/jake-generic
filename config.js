// relative paths to project directories
const app_dir = './app'
const spec_dir = './test'

module.exports = {
  "name" : "generic jakefile",
  "dir"  : `${app_dir}`,
  "spec" : `${spec_dir}`,
  "scripts"  : `${app_dir}/scripts`,
  "styles"   : `${app_dir}/styles`,
  "markup"   : `${app_dir}/templates`,
  "images"   : `${app_dir}/images`,
  "git": "https://github.com/thelostspore/generic-jakefile"
}
