# jake-generic
A general-purpose Jakefile with sample app.

## Technologies
- `node` v4.2.4
- Depdencies management: `npm`
- Syntax: `jshint`
- Tests: `mocha` with `chai`

## Instructions
```bash
git clone
cd $_
npm install
jake <task>
```
